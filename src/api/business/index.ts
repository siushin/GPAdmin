// 业务相关 API
// 后续业务 API 可以在这里添加
export const businessApi = {
  // 示例：业务相关接口
  // import request from '@/utils/request'
  // import type { ApiResponse } from '@/types'
  // getBusinessData(): Promise<ApiResponse<any>> {
  //   return request.get('/business/data')
  // }
}
