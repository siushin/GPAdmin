import { PageContainer } from '@ant-design/pro-components';
import { Empty } from 'antd';
import React from 'react';

const Organization: React.FC = () => {
  return (
    <PageContainer>
      <Empty description="组织架构功能开发中..." />
    </PageContainer>
  );
};

export default Organization;
